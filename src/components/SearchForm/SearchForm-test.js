import React from 'react';
import SearchForm from './SearchForm';
import '@testing-library/jest-dom';

describe('SearchForm', () => {
  it('Displays a search form that we expect', () => {
    expect(true).toEqual(true);
  });
});
